import { DonationReportingDirective } from './donation-reporting.directive';

describe('DonationReportingDirective', () => {
  it('should create an instance', () => {
    const directive = new DonationReportingDirective();
    expect(directive).toBeTruthy();
  });
});
