import { CampManagementDirective } from './camp-management.directive';

describe('CampManagementDirective', () => {
  it('should create an instance', () => {
    const directive = new CampManagementDirective();
    expect(directive).toBeTruthy();
  });
});
