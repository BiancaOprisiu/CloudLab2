import { PermissionManagementDirective } from './permission-management.directive';

describe('PermissionManagementDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionManagementDirective();
    expect(directive).toBeTruthy();
  });
});
