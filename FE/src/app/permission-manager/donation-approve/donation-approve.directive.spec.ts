import { DonationApproveDirective } from './donation-approve.directive';

describe('DonationApproveDirective', () => {
  it('should create an instance', () => {
    const directive = new DonationApproveDirective();
    expect(directive).toBeTruthy();
  });
});
