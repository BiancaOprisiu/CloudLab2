import { DonationManagementDirective } from './donation-management.directive';

describe('DonationManagementDirective', () => {
  it('should create an instance', () => {
    const directive = new DonationManagementDirective();
    expect(directive).toBeTruthy();
  });
});
