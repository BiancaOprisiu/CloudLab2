import { BenefManagementDirective } from './benef-management.directive';

describe('BenefManagementDirective', () => {
  it('should create an instance', () => {
    const directive = new BenefManagementDirective();
    expect(directive).toBeTruthy();
  });
});
