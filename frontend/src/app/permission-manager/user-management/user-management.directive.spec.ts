import { UserManagementDirective } from './user-management.directive';

describe('UserManagementDirective', () => {
  it('should create an instance', () => {
    const directive = new UserManagementDirective();
    expect(directive).toBeTruthy();
  });
});
