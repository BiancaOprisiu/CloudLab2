import { CampReportingDirective } from './camp-reporting.directive';

describe('CampReportingDirective', () => {
  it('should create an instance', () => {
    const directive = new CampReportingDirective();
    expect(directive).toBeTruthy();
  });
});
